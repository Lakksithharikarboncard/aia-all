import { LeadCaptureForm } from "@/components/customer/LeadCaptureForm";

export const metadata = {
  title: "Get Started — AI Accountant",
  description: "Share your billing needs and we'll recommend the right plan.",
};

export default function GetStartedPage() {
  return <LeadCaptureForm />;
}
